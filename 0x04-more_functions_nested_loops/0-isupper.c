#include "main.h"

/**
 * _isupplier - Check if letter is upper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 for any else
 */
int _isupplier(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return(0);
}
